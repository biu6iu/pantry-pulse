import { ContributionSection, ImpactSection } from "@/components/ui/ui";

export default function Home() {
    return (
        <main>
            <ImpactSection />
            <ContributionSection />
        </main>
    );
}